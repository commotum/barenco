# Paper-to-Lean Traceability

Primary source: `Barenco/barenco-1995.pdf` (arXiv `quant-ph/9503016v1`). Markdown
line numbers refer to `Barenco/barenco-1995.md` and are navigation aids only.

## Status Vocabulary

- **planned** — in the accepted project scope but not yet formalized.
- **proved as stated** — source claim and assumptions are represented faithfully.
- **corrected and proved** — a documented repaired statement is proved.
- **additional assumptions** — proved with assumptions absent or implicit in source.
- **partial** — some verification layers are complete; the row names which ones.
- **excluded** — intentionally not theoremized, with a documented reason.
- **unresolved** — exact obstruction is known or investigation remains open.

Verification layers use `A` algebraic identity, `C` full circuit correctness, `W`
wire/ancilla preservation, `R` exact structural resources, and `O` asymptotics.

## Foundations and Notation

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| D2-control | pp. 6–7; lines 88–138 | Definition and lower-right-block matrix for `∧ₘ(U)` | `positiveControlledRaw`, `positiveControlledUnitary`, `positiveControlledRaw_truthTable`; paper-order wrapper/lexicographic block theorem planned | A,C,W | partial: arbitrary positive control sets/targets, exact basis action, untouched wires, and unitarity proved; source-row and `Fin (2^n)` matrix presentation remains |
| D2-Toffoli | p. 6; lines 127–138 | `∧ₘ(σx)` Boolean action | `pauliX`, `xRaw_mulVec_basisKet`, positive-control specialization; `cnotRaw_mulVec_basisKet` for `m=1` | A,C,W | partial: reusable general semantics proved; named all-controls paper wrapper and lexicographic theorem remain |
| C2-dense | pp. 6–7; lines 146–150 | cited claim that “almost any” fixed controlled-U densely generates | no local theorem from this paper | A,C,O | intentionally excluded from paper reconstruction: source cites [29,31] and gives no proof; exact universality with all U(2) primitives is handled separately |
| N3-diagrams | pp. 7–8; lines 158–168; image `notation-basic-gates.png` | wire/control/target notation; time left-to-right | `evalGates_pair`, `fromPaper_paperProduct`, `Circuit.eval_append`, `Primitive.oneQubit`, `Primitive.cnot` | C | corrected and proved for chronology, typed one-qubit/CNOT wires, and source/semantic translation; later diagram-specific circuits remain in their rows |
| N3-basic | p. 8; line 166 | “basic” = arbitrary one-qubit or CNOT | private-constructor `Primitive`; trusted `.oneQubit`/`.cnot`; `Circuit.registerWidth`, `gateCount`, `kindCount`, `touchedSupport`, `cost`; `CostModel.oneQubitCNOT`; lower-bound-only proof-carrying `LowerBounds.BasicPrimitive`/`BasicCircuit` with erasure, evaluator, count, and cost bridges | C,R | corrected and proved at the syntax/cost-model layer: generic support is bounded by ambient width and unsupported kinds make cost `none`; Lemma 7.7 quantifies over the exact restricted constructors, so semantic locality is not inferred from generic kind metadata; no paper decomposition count follows until its circuit exists |
| F3-phase | §6.2, pp. 15–16; lines 552–584 | informal “congruent modulo phase shifts” language | `GlobalPhaseEq`, input-column `BasisPhaseEq`, `SameBasisBehavior`, `BasisMeasurementEq` and implication/equivalence laws; Section 6 witnesses `relativeToffoliPhase` and `controlledWPhase` | A | corrected and proved for the Section 6 operators: the relations remain distinct, the diagrams use the `101` input-column sign, controlled-`W` uses `111`, and only adjacent identical relative-Toffoli pairs are currently proved to cancel |
| F3-measure | §6.2 and pp. 22–23; lines 552–584, 770–774 | phase-sensitive quantum use and measurement-probability discussion | `conjugationChannel`, `ChannelEq`, arbitrary-matrix/effect `BornWeight` and `AllMeasurementEq`; `channelEq_iff_allMeasurementEq`; `ChannelEq.toBasisMeasurementEq`; `operatorDistance_basisOutcomeProbability_le`; `eventProbability`; `operatorDistance_eventProbability_le`; `operatorDistance_eventProbability_le_two_mul` | A | corrected clarification proved algebraically: global phase cancels and all-matrix/effect equality separates channels. For unitary images of a common norm-at-most-one pure input, every finite computational-basis event has a cardinality-free constant-one operator-distance bound, which implies the paper-facing constant-two bound. This is not an arbitrary-POVM theorem, and unrestricted matrices are not declared physical states/effects. |

## Section 4 — Matrix Properties

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| L4.1-SU | pp. 8–9; lines 172–267 | exact `Rz Ry Rz` form for every SU(2) matrix | `QubitSpecialUnitary`; `specialUnitary_canonical`; `paperEuler_entry_formula`; `specialUnitary_exists_paperEuler`; `specialUnitary_exists_columnEuler`; `specialUnitary_exists_rz_mul_ry_mul_rz` | A | corrected and proved: total `Complex.arg` choices cover zero entries, the middle angle lies in `[0,π]`, and paper-row versus semantic-column outer-factor order is explicit |
| L4.1-U2 | pp. 8–9; lines 172–267 | scalar phase times an SU(2) Euler form for every U(2) matrix | `determinantPhaseAngle`, `specialUnitaryPart`, `phaseShift_mul_specialUnitaryPart`, `unitary_exists_paperPhase_mul_paperEuler`, `unitary_exists_phaseShift_mul_rz_mul_ry_mul_rz`; `paperPhase_pi_mul_paperRz` | A | corrected and proved: the principal half-argument branch is explicit, reconstruction is exact, and the possible scalar `-I` is absorbed by a `2π` Z-angle shift rather than silently setting the phase to zero |
| L4.2 | p. 9; line 269 | six `Ry`, `Rz`, scalar-phase, and Pauli-X identities | raw `paperRy_mul`, `paperRz_mul`, `paperPhase_mul`, `paperX_sq`, and two `paperX` conjugation theorems; semantic `ry_mul`, `rz_mul`, `phaseShift_mul`, `sigmaX_sq`, and two `sigmaX` conjugation theorems; certified gate packages | A | proved as stated for all real parameters in both conventions; exact matrix identities only, with no circuit syntax or resource conclusion |
| L4.3 | pp. 9–10; lines 278–341 | `A B C = I` and `A X B X C = W` for `W∈SU(2)` | `paperA`, `paperB`, `paperC` and SU certificates; parameterized raw/column identities; `specialUnitary_exists_paperABC`; `specialUnitary_exists_columnChronologicalABC` | A | proved as stated at the algebraic layer: raw order is `A B C`, while chronological standard-column semantics is the reversed `Cᵀ Bᵀ Aᵀ`; no `Circuit` or count theorem follows |
| I4-root | root choices used at pp. 14–24; lines 530–544, 718–722, 782–827 | existence of square, iterated, and approximation roots | finite-index `unitaryRoot`, `unitaryRoot_pow`, `exists_unitary_pow_eq`, `unitarySquareRoot_pow_two`, `unitaryRoot_pow_two_pow`; coherent `powerTwoRoot`, `powerTwoRoot_zero`, `powerTwoRoot_succ_sq`, `powerTwoRoot_operatorDistance_one_le` | A | corrected and proved in a stronger arbitrary-finite-index form: every positive root is exact, the named power-of-two sequence has exact adjacent squaring, and its L² operator distance to identity is at most `pi/2^m`. The shared principal spectral branch is explicit; no continuity of the selected root as the input matrix varies is claimed. |

## Section 5 — Two-Bit Networks

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| L5.1 | pp. 10–11; line 347; image `lemma-5-1-controlled-su2.png` | two-CNOT/three-one-qubit circuit iff target is SU(2) | `controlledABCCircuit`; `eval_controlledABCCircuit_eq_iff`; `controlledSU2Circuit_correct_iff`; `controlledABCCircuit_gateCount`; `controlledABCCircuit_kindCounts`; `controlledABCCircuit_oneQubitCNOTCost` | A,C,W,R | proved in both directions as exact arbitrary-register circuit equality; the converse derives `det W=1` from the active branch and both `det X=-1` factors; syntax cost is exactly three one-qubit plus two CNOT gates |
| L5.2 | p. 11; line 361; image `lemma-5-2-controlled-phase.png` | controlled scalar phase equals a phase gate on control | `controlPhase`; `controlPhase_eq_matrix2`; `controlledScalarUnitary_eq_localControl`; `controlledPhaseCircuit`; `eval_controlledPhaseCircuit`; `controlledPhaseCircuit_oneQubitCNOTCost` | A,C,W,R | proved exactly for arbitrary ambient width: controlled `Ph(delta)` is the local control gate `diag(1,cis delta)`, not an ignorable global phase; circuit cost is one one-qubit gate |
| C5.3 | p. 11; line 391 | controlled U(2) uses ≤4 one-qubit + 2 CNOT | `controlledU2Circuit`; `eval_controlledU2Circuit_of_products`; `controlledU2Circuit_exists`; `controlledU2Circuit_gateCount`; `controlledU2Circuit_kindCounts`; `controlledU2Circuit_oneQubitCNOTCost` | C,W,R | proved as a constructed exact upper bound: six syntax occurrences, exactly four one-qubit and two CNOT, with cost `some 6` under `oneQubitCNOT` |
| L5.4 | pp. 12–13; line 408; image `lemma-5-4-two-xor-special-case.png` | characterization of two-CNOT/two-inverse-gate family | `twoCNOTCircuit`; `eval_twoCNOTCircuit_eq_iff`; `symmetricEuler`; `sigmaX_mul_star_mul_sigmaX_mul_eq_symmetricEuler`; `twoCNOTFamily_iff`; `twoCNOTCircuit_oneQubitCNOTCost` | A,C,W,R | corrected and proved in both directions, including zero phase-carrying coordinates; column family is `rz alpha * ry theta * rz alpha`; exact syntax cost is two one-qubit plus two CNOT gates; the source's illustrative `Rx` exclusion has the scalar exceptions recorded in C-018 |
| L5.5 | p. 13; line 471; image `lemma-5-5-one-xor-special-case.png` | characterization of one-CNOT family | `oneCNOTCircuit`; `eval_oneCNOTCircuit_eq_iff`; `pauliConjugate_eq_sigmaX_mul_symmetricEuler`; `oneCNOTSpecialFamily_iff`; `unitaryPauliConjugate_eq_specialUnitaryPart`; `oneCNOTFamily_iff`; `oneCNOTCircuit_oneQubitCNOTCost` | A,C,W,R | corrected and proved in both directions: the written source proof explicitly supplies only the construction direction; Lean derives `B=A⁻¹` from an arbitrary circuit's inactive branch and phase-normalizes arbitrary U(2) witnesses for the converse; column family is `X * symmetricEuler`; cost is two one-qubit plus one CNOT |
| C5.6 | p. 14; line 512 | controlled U from four one-qubit + two controlled special-family gates | `controlledVMacroU2Circuit`; `controlledVMacroU2Circuit_exists`; `controlledVMacroU2Circuit_gateCount`; `controlledVMacroU2Circuit_kindCounts`; `controlledVMacroU2Circuit_oneQubitCNOTCost`; `expandedVMacroU2Circuit`; `eval_expandedVMacroU2Circuit_eq_macro`; `eval_expandedVMacroU2Circuit_eq_controlledU2Circuit`; `expandedVMacroU2Circuit_gateCount`; `expandedVMacroU2Circuit_kindCounts`; `expandedVMacroU2Circuit_oneQubitCNOTCost`; `expanded_and_mergedVMacroU2Circuit_oneQubitCNOTCosts` | C,W,R | corrected and proved in three distinct layers: the enlarged-library macro has 6 nodes (4 one-qubit + 2 controlled-`V`) and `oneQubitCNOT` cost `none`; its primitive expansion has 10 nodes (8 one-qubit + 2 CNOT) and cost `some 10`; three proved local merge groups give evaluator equality with the distinct 6-node `controlledU2Circuit` (4 one-qubit + 2 CNOT), whose cost is `some 6` |
| N5-Z | p. 14; image `controlled-z-symmetry.png` | controlled-Z symmetric under wire swap | `sigmaZUnitary`; `controlledZRaw_truthTable`; `controlledZRaw_swap`; `controlledZUnitary_swap` | A,C,W | proved as exact arbitrary-register equality; the relative sign occurs exactly when both named wires are true |

## Section 6 — Three-Bit Networks

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| L6.1 | pp. 14–15; line 526; image `lemma-6-1-controlled-controlled-u.png` | exact doubly controlled U from V, V†, CNOT with `V²=U` | `twoControlSet`; `doubleControlledViaSquareCircuit`; `eval_doubleControlledViaSquareCircuit_pow_two`; `eval_doubleControlledViaSquareCircuit_of_sq_eq`; `doubleControlledRootCircuit`; `eval_doubleControlledRootCircuit`; `doubleControlledViaSquareCircuit_gateCount`; `doubleControlledViaSquareCircuit_cnotCount`; `doubleControlledViaSquareCircuit_controlledCount`; `doubleControlledViaSquareCircuit_oneQubitCNOTCost` | A,C,W,R | corrected and proved exactly for arbitrary ambient width and three pairwise distinct wires: the certified root choice is supplied, both inverse orders are checked, the second control and spectators are restored by full-register equality, and the five-node macro has three controlled gates plus two CNOTs but `oneQubitCNOT` cost `none` |
| C6.2 | p. 15; line 548 | ≤8 one-qubit + 8 CNOT | `doubleControlledExpansion20Circuit`; `doubleControlledExpansion16Circuit`; `eval_doubleControlledExpansion20Circuit_eq_16`; `eval_doubleControlledExpansion16Circuit_of_products`; `doubleControlledExpansion16Circuit_exists`; `doubleControlledUnitary_has_sixteenPrimitiveCircuit`; `doubleControlledExpansion20Circuit_kindCounts`; `doubleControlledExpansion20Circuit_oneQubitCNOTCost`; `doubleControlledExpansion16Circuit_kindCounts`; `doubleControlledExpansion16Circuit_oneQubitCNOTCost` | C,W,R | corrected and proved as an exact constructed upper bound: one shared factorization gives a 20-node circuit (12 one-qubit + 8 CNOT), proved disjoint-wire commutations remove two inverse pairs, and the named result has exactly 8 one-qubit + 8 CNOT with cost `some 16` |
| U6.2-W | pp. 15–16; lines 552–566 | `W = Ph(pi/2) sigma_y`; `∧₂(W)` has Toffoli basis behavior with a relative sign | `paperY`; `sigmaY`; `paperW`; `paperW_eq_paperPhase_mul_paperY`; `wMatrix`; `wMatrix_eq_sigmaY_mul_phaseShift`; `wMatrix_eq_phaseShift_mul_sigmaY`; `wMatrix_eq_ry_pi`; `wUnitary_eq_phaseShift_mul_sigmaY`; `controlledWUnitary`; `controlledWPhase_input`; `controlledWUnitary_mulVec_basisKet`; `controlledWUnitary_basisPhaseEq_toffoli` | A,C,W | corrected and proved with exact row- and column-convention matrix identities plus an arbitrary-width signed action: transposition changes the antisymmetric Pauli-Y sign and reverses the product before scalar commutativity restores the displayed order; the original PDF's “latter” attribution is false, and controlled-`W` has the Toffoli-relative minus sign exactly on input `111`, not one global phase |
| U6.2-A | p. 16; lines 567–575; image `relative-phase-toffoli-a.png` | A-rotation relative-phase Toffoli circuit | `relativePhaseToffoliACircuit`; `eval_relativePhaseToffoliACircuit`; `relativePhaseToffoliACircuit_mulVec_basisKet`; `relativePhaseToffoliACircuit_basisPhaseEq_toffoli`; `eval_append_relativePhaseToffoliACircuit_self`; `relativePhaseToffoliACircuit_gateCount`; `relativePhaseToffoliACircuit_oneQubitCount`; `relativePhaseToffoliACircuit_cnotCount`; `relativePhaseToffoliACircuit_oneQubitCNOTCost` | A,C,W,R | proved as stated and strengthened to exact arbitrary-width semantics: the Toffoli-relative minus sign is exactly on input `101`; the syntax has four one-qubit plus three CNOT gates and cost `some 7`; only immediately adjacent identical copies are proved to cancel |
| U6.2-B | p. 16; lines 577–583; image `relative-phase-toffoli-b.png` | alternate B-rotation circuit with same phases | `relativePhaseToffoliBCircuit`; `eval_relativePhaseToffoliBCircuit`; `eval_relativePhaseToffoliACircuit_eq_BCircuit`; `relativePhaseToffoliBCircuit_mulVec_basisKet`; `relativePhaseToffoliBCircuit_basisPhaseEq_toffoli`; `eval_append_relativePhaseToffoliBCircuit_self`; `relativePhaseToffoliBCircuit_gateCount`; `relativePhaseToffoliBCircuit_oneQubitCount`; `relativePhaseToffoliBCircuit_controlledZMacroCount`; `relativePhaseToffoliBCircuit_oneQubitCNOTCost` | A,C,W,R | proved as stated and strengthened to exact equality with the A circuit; the source syntax has four one-qubit plus three controlled-Z macros, gate count seven, and `oneQubitCNOT` cost `none`, so the A circuit's basic-gate cost is not assigned to this different syntax |

## Section 7 — n-Bit Networks

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| U7-Gray4 | p. 17; lines 585–614; image `four-bit-gray-code-construction.png` | four-bit Gray/parity controlled-U construction | `fourBitGrayCircuit`; `grayControlledViaRootCircuit_two_eq_fourBitGrayCircuit`; `eval_fourBitGrayCircuit`; `eval_fourBitGrayCircuit_of_pow_four_eq`; exact macro counts | A,C,W,R | proved as displayed: the generated syntax is exactly the 13-node chronology, has seven controlled-root macros and six CNOTs, and is correct in every ambient register; early-basic expansion remains separate |
| L7.1 | pp. 17–18; line 616 | general omitted Gray-code construction; exact gate counts | `grayControlledViaRootCircuit`; `eval_grayControlledViaRootCircuit`; `grayControlledCircuit`; `eval_grayControlledCircuit`; `expandedGrayControlledCircuit`; `eval_expandedGrayControlledCircuit`; exact macro and primitive count/cost theorems | A,C,W,R,O | corrected and proved: the omitted interleaved invariant and exact restoration are reconstructed; theorem is strengthened from the paper's range to every positive control count, while zero controls require a separate local-gate base. The schedule-aware literal expansion has `4(2^m−1)` one-qubit gates, `3·2^m−4` CNOTs, and total/cost `7·2^m−8` for `m` controls. Its CNOT count matches the source; the smaller printed one-qubit count would require cross-block mergers not present in this raw syntax and is not inferred from semantic equality. |
| L7.2 | pp. 18–19; line 668; image `lemma-7-2-linear-multi-control.png` | dirty-wire multi-X from `4(m−2)` Toffolis | `InwardLadderLayout`; `halfLadderCircuit`; `inwardLadderCircuit`; `inwardLadderUpdate_eq_update`; `eval_inwardLadderCircuit`; exact gate/Toffoli counts; capacity/support/cost theorems | C,W,R | corrected and proved: `b+1=m−2>0` dirty wires and `m=b+3≥3` controls are explicit, all borrowed/spectator wires are restored for arbitrary ambient inputs, and exact full-register equality covers superposition/entanglement; count is `4(b+1)=4(m−2)` from syntax; layout capacity is `2m−1≤n`; Toffoli macros retain no early-basic cost before expansion |
| L7.3 | pp. 19–20; line 682; image `lemma-7-3-four-block-construction.png` | four-block dirty-wire construction | `FourBlockLayout`; `fourBlockCircuit`; `fourBlockUpdate_eq_update`; `eval_fourBlockCircuit`; `fourBlockSubstitutionCircuit`; exact syntax/cardinality/count theorems | C,W,R | corrected and proved: the source's “by inspection” is replaced by explicit Boolean-ring algebra and exact arbitrary-width operator equality; the unknown dirty wire and every spectator are restored, the legal split is encoded without truncated subtraction, and the syntax has exactly four macros; checked A/B implementations may be substituted with exact doubled counts, while Corollary 7.4's ladder instantiation remains separate |
| C7.4 | p. 20; line 688 | `8(n−5)` Toffolis and `48n−204` early-basic upper bound | exact macro layer above; `relativeHalfLadderCircuit`; `relativeInwardPhaseExponent`; `hybridInwardLadderCircuit`; `relativeCorollary74Circuit`; exact contextual/occurrence theorems; `exactToffoliExpansionCircuit`; `expandedRelativeCorollary74Circuit`; `balancedExpandedRelativeCorollary74Circuit`; exact raw component/total/cost theorems; diagnostics | C,W,R,O | corrected and proved through literal primitive expansion: the repaired floor partition gives exact full-register semantics and `8(n−5)` total occurrences; the actual `101` phase makes two forward A copies false, so the checked chronology is `Arel;Bhybrid;adjoint(Arel);Bhybrid`, with four exact and `8n−44` relative occurrences; four selected checked sixteen-node expansions yield exactly `32n−144` one-qubit gates, `24n−100` CNOTs, and raw cost `56n−244`, including `(80,68,148)` at `n=7`; the printed optimized `48n−204` is not derived by the source or current explicit normalization and remains unresolved rather than exported |
| L7.5 | p. 21; line 718; image `lemma-7-5-quadratic-general-control.png` | recursive fully controlled U using square root | `recursiveViaSquareCircuit`; `eval_recursiveViaSquareCircuit_pow_two`; `recursiveRootCircuit`; `eval_recursiveRootCircuit`; `zeroControlCircuit`; `recursiveSubstitutionCircuit`; `expandedRecursivePrefixXCircuit`; `recursivePrimitiveCircuit`; `eval_recursivePrimitiveCircuit`; exact recurrence/count/cost theorems | A,C,W,R | corrected and proved through primitive syntax for arbitrary ambient width: the exact five-node chronology is reconstructed, all controls split into prefix plus last, the selected square root is exact, `prefix=0` gives a valid one-control boundary, and zero controls use a separate local circuit. The explicit recursion stops at the six-control Gray expansion; each larger prefix-controlled X borrows and restores the original unitary target, and the final evaluator is exact on the complete register, including spectators and external entanglement. |
| C7.6 | pp. 21–22; line 724 | claimed `Θ(n²)` and `48n²+O(n)` | `recursivePrimitiveOneQubitCount`; `recursivePrimitiveCNOTCount`; `recursivePrimitiveTotalCount`; circuit-linkage and successor theorems; Nat-safe width formulas; `recursivePrimitiveTotalCount_isBigOWith_width`; `recursivePrimitiveTotalCount_isBigO_width` | R,O | corrected and proved for the named construction: with `d=n−7`, exact counts are `32d²+200d+252` one-qubit, `24d²+164d+188` CNOT, and `56d²+364d+440` total/cost. At `n≥7`, the Nat-safe total is `56n²+636−420n`, the exact width increment is `112n−364`, and the real-cast count is `IsBigOWith 56` of `n²`. This is an explicit quadratic upper bound, not an optimal-synthesis `Θ` result; coefficient 56 reflects the proved raw Corollary 7.4 expansion, while the paper's coefficient 48 depends on its unresolved normalization. |
| L7.7 | p. 22; line 750 | nonscalar fully controlled U needs ≥`n−1` basic operations; proof actually counts XORs | `BasicPrimitive`; `BasicCircuit`; exact erasure/evaluator/count/cost bridges; `interactionGraph`; `interactionGraph_edgeFinset_card_le_cnotCount`; `cnotCount_lowerBound_of_interactionGraph_connected`; `wireSplit`; `partitionReindex`; `TensorFactorsAcross`; `eval_tensorFactorsAcross_targetComponent`; `IsScalarQubitMatrix`; `isScalarQubitMatrix_coe_iff_exists_phaseShiftUnitary`; `not_tensorFactorsAcross_fullyControlled_of_not_scalar`; `interactionGraph_connected_of_eval_fullyControlled`; `fullyControlled_cnotCount_lowerBound`; `fullyControlled_cnotCount_lowerBound_of_not_exists_phaseShiftUnitary`; `fullyControlled_gateCount_lowerBound`; `fullyControlled_oneQubitCNOTCost_lowerBound` | A,C,W,R | corrected and proved more strongly: an exact same-register implementation using arbitrary one-qubit gates and CNOTs requires at least `n−1` CNOT occurrences, hence at least that many total basic gates and accepted named cost. Duplicate CNOT edges, arbitrary noncontiguous cuts, Kronecker reindexing, and the suppressed nonscalar obstruction are explicit. The theorem has no ancilla, approximation, measurement, or phase-relaxed equality; scalar-phase targets are excluded exactly, the one-wire bound is zero, and Section 5/6 named-topology results remain upper/classification theorems rather than global minima; see C-027. |
| D7-approx | pp. 22–23; line 770 | distance induced by the Euclidean vector norm | `operatorDistance` using scoped `Matrix.Norms.L2Operator`; metric, multiplication, unitary-invariance, product-error, and state-action bounds; `norm_targetBlockRaw`; `operatorDistance_positiveControlledUnitary_one_eq` | A | corrected and proved for the exact L² induced operator norm. Target-block assembly is isometric, so the distance between a positive-controlled target and identity is exactly the one-qubit target distance whenever the positive control has its all-true active block. No Frobenius or entrywise norm is substituted. |
| P7-approx | pp. 22–23; line 772 | probabilities of any measured event differ by ≤`2ε` | `eventProbability`; `eventProjection`; `eventReflection`; `operatorDistance_eventProbability_le`; `operatorDistance_eventProbability_le_two_mul`; `epsilonSynthesisPrimitiveCircuit_eventProbability_le`; `epsilonSynthesisPrimitiveCircuit_eventProbability_le_two_mul` | A | corrected and proved for every finite computational-basis event and a common pure input of norm at most one. The cardinality-free constant-one theorem is stronger than the paper's valid constant-two corollary. The formal claim is deliberately not generalized to arbitrary POVMs. |
| L7.8 | pp. 23–24; line 774 | recursive-root approximate controlled U in claimed `Θ(n log(1/ε))` | `powerTwoRoot_succ_sq`; `powerTwoRoot_operatorDistance_one_le`; `truncatedRecursiveCircuitFrom`; `positiveControlledUnitary_eq_residual_mul_eval_truncatedFrom`; `operatorDistance_truncatedRecursiveCircuitFrom_eq_residual`; `expandedTruncatedRecursiveCircuit`; `operatorDistance_expandedTruncatedRecursiveCircuitFrom_eq_residual`; exact expanded component/count/cost theorems; `principalRootBoundDepth`; `epsilonSynthesisPrimitiveCircuit`; `operatorDistance_epsilonSynthesisPrimitiveCircuit_le`; `epsilonSynthesisPrimitiveCircuit_gateCount`; `epsilonSynthesisPrimitiveCircuit_oneQubitCNOTCost`; `epsilonSynthesisTotalCountAtWidth_le`; `epsilonSynthesisTotalCountAtWidth_lt_logarithmic` | A,C,R,O | corrected and proved as a literal one-qubit/CNOT construction for logical source width `n≥7`, with arbitrary ambient spectators. For residual exact depth `r` and retained depth `k`, exact counts are `32k²+(64r+200)k` one-qubit, `24k²+(48r+164)k` CNOT, and `56k²+(112r+364)k` total/cost. The natural-ceiling depth is used only when it fits; otherwise an exact circuit is selected. The uniform count is at most `440+112n·min(principalRootBoundDepth ε,n−7)`, and an explicit logarithmic-regime upper bound is proved for `0<ε≤1`. This is a construction-specific upper theorem, not the source's unrestricted or optimal `Θ(n log(1/ε))`; see C-006–C-008. |
| L7.9 | p. 24; line 854; image `lemma-7-9-linear-su2-control.png` | multi-control SU(2) ABC circuit | `linearABCCircuit`; `linearABCTargetProduct_eq`; `eval_linearABCCircuit_of_products`; `linearSU2Circuit`; `eval_linearSU2Circuit`; `expandedLinearSU2Circuit`; `eval_expandedLinearSU2Circuit`; exact component/total/cost theorems | A,C,W,R | proved as stated and strengthened: the five-macro chronology and all four control branches are exact on arbitrary ambient registers, including the empty-prefix semantic boundary. The selected primitive expansion requires logical width `n=p+2≥7` and has exact profile `(64n−279,48n−194,112n−473)`. |
| C7.10 | p. 24; line 862 | claimed linear `∧_{n−2}(W)` construction | L7.9 syntax above; `linearSU2OneQubitCountAtWidth`; `linearSU2CNOTCountAtWidth`; `linearSU2TotalCountAtWidth`; exact circuit-resource links; `linearSU2TotalCount_isBigOWith_width`; `fullyControlledWPhase`; `fullyControlledW_basisPhaseEq_pauliX`; expanded phase-relaxed consequences | C,W,R,O | corrected and proved: the printed subscript is repaired to the intended fully controlled `∧_{n−1}(W)`; for `n≥7` the named construction has exact count `112n−473` and is `IsBigOWith 112`/`O(n)`, with no optimal `Θ(n)` claim. The source's `W` differs from controlled X by a minus sign exactly when all controls and the input target are one, so the valid conclusions are `BasisPhaseEq`, basis behavior, and basis measurement equivalence—not global phase equality. |
| L7.11 | p. 25; line 879; image `lemma-7-11-one-fixed-bit.png` | general controlled U with one zero fixed/restored wire | `fixedWireSubspace`; `cleanZeroSubspace`; `cleanZeroLinearEquiv`; `cleanAncillaCircuit`; `eval_cleanAncillaCircuit_mulVec_basisKet`; both auxiliary-branch theorems; `eval_cleanAncillaCircuit_mulVec_of_mem_cleanZero`; output-closure/factorization theorems; expanded analogues | C,W,R | corrected and proved: the exact general firing condition is `aux xor conjunction(data)`; intended controlled-U behavior holds on the literal clean-zero subspace for arbitrary data, target, and spectator superpositions, not as equality of full unitaries. Output closure and explicit fixed-wire factorization prove restoration with no residual entanglement. |
| C7.12 | p. 25; line 887 | claimed linear construction with fixed wire | `expandedCleanAncillaCircuit`; `expandedCleanAncillaCircuit_oneCleanAncillaContract`; exact logical-width counts/cost; `cleanAncillaOneQubitCountAtWidth`; `cleanAncillaCNOTCountAtWidth`; `cleanAncillaTotalCountAtWidth`; exact resource links; `cleanAncillaTotalCount_isBigOWith_width` | C,W,R,O | corrected and proved: exactly one clean-zero wire is declared; for `n≥7` the primitive profile is `(64n−284,48n−198,112n−482)` and the count is `IsBigOWith 112`/`O(n)`. Clean-subspace output closure justifies reuse; no matching lower bound or optimal `Θ(n)` claim is exported. |

## Section 8 — General Synthesis and Changed Cost Model

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| N8-basic | p. 26; lines 891–895 | “basic” changes to arbitrary two-qubit gate | `CostModel.arbitraryTwoQubit`; `CostModel.arbitraryTwoQubit_controlled_eq_some_one_iff`; `CostModel.arbitraryTwoQubit_controlled_eq_none_iff`; `Primitive.arbitraryTwoQubit_cost_positiveControlled`; `Circuit.arbitraryTwoQubit_cost_of_oneQubitCNOT_cost` | R | corrected and proved as a syntax-only at-most-two-wire model: `.controlledOneQubit controls` costs one exactly when `controls ≤ 1`, while arity at least two, Toffoli macros, and unclassified kinds remain rejected. The one-qubit/CNOT and Section 8 models stay distinct; the bridge preserves the exact cost only for literal syntax already accepted by the stricter model. Pricing metadata does not create an arbitrary-two-qubit smart constructor. |
| U8-Toffoli5 | p. 26; lines 895–900 | exact Toffoli in five arbitrary two-qubit gates | `eval_doubleControlledRootCircuit`; `doubleControlledViaSquareCircuit_arbitraryTwoQubitCost`; `doubleControlledRootCircuit_arbitraryTwoQubitCost` | C,R | proved as a constructed exact upper count: the five literal Lemma 6.1 nodes are all at-most-two-wire operations and have exact Section 8 cost `5` on an arbitrary ambient register. The semantic theorem handles every doubly controlled one-qubit `U`, hence includes the `U = sigmaXUnitary` Toffoli specialization. No five-gate minimality claim is made. |
| U8-relToffoli3 | p. 26; lines 895–900 | relative-phase Toffoli in three after merging | `relativePhaseToffoliACircuit_basisPhaseEq_toffoli`; `relativePhaseToffoliBCircuit_basisPhaseEq_toffoli`; `relativePhaseToffoliACircuit_arbitraryTwoQubitCost`; `relativePhaseToffoliBCircuit_arbitraryTwoQubitCost` | A,C,R | partially formalized, with the optimization unresolved: both verified unmerged circuits establish the explicit basis-dependent-phase relation to Toffoli and each has exact Section 8 cost `7`. The source's cost `3` requires an explicit merged three-node certified two-wire circuit and evaluator-preservation proof; neither exists, so the existing seven-node syntax is not silently repriced. |
| U8-Toffoli4-13 | p. 26; lines 895–900 | four-bit Toffoli in thirteen arbitrary two-qubit gates | `eval_fourBitGrayCircuit`; `eval_fourBitGrayCircuit_of_pow_four_eq`; `fourBitGrayCircuit_kindCounts`; `fourBitGrayCircuit_arbitraryTwoQubitCost` | C,R | proved as a constructed exact upper count: the displayed circuit contains seven singly controlled roots and six CNOTs, implements the three-control target operation when `V ^ 4 = U`, and has exact Section 8 cost `13`. Taking `U` to be Pauli-X gives the four-bit Toffoli instance; no thirteen-gate minimality claim is made. |
| M8-minimal | p. 26; lines 899–902 | numerical evidence that preceding counts are minimal | none unless a new exhaustive proof is supplied | R | excluded as paper theorem; source explicitly has no proof |
| U8-six | pp. 26–27; lines 903–933; image `six-two-bit-gates-u8.png` | six arbitrary U(4) gates allegedly realize every U(8) | candidate architecture only (planned investigation) | A,C,R | unresolved; dimension 64 does not prove surjectivity |
| H8-dim | pp. 26–27; lines 909–936 | parameter increments 16,12,9,… | documented parameter heuristic | R,O | excluded pending manifold/image-dimension proof |
| H8-lower | p. 27; lines 931–937 | conjectural `(4^n−3n−1)/9` two-qubit lower bound | none as unconditional theorem | R,O | excluded; paper calls it a conjecture based on dimension counting |
| U8-twolevel | p. 27; lines 939–957 | unitary decomposition into two-level rotations and diagonal phases | `givensUnitary`; `FinTwoLevelDecomposition`; `decomposeFinUnitary`; `FiniteTwoLevelDecomposition`; `decomposeFiniteUnitary` and product/diagonal theorems | A | corrected and proved: constructive exact left-Givens elimination returns an explicit conventional matrix-product list and a certified diagonal residual satisfying `U = finiteFactorProduct factors * residual`; no phase is discarded, dimensions zero and one are covered, and a proved reindex bridge handles every finite decidable index type. |
| U8-diagonal | p. 27; lines 947–957 | absorb/synthesize the remaining diagonal phases | `diagonalUnitary_entry_star_mul`; `diagonalPatternBlock`; `diagonalCircuit`; `eval_diagonalCircuit`; `allComplementPatterns_length`; `pow_two_mul_sq_succ_le_diagonalCircuitCost`; `diagonalCircuitCost_le_pow_two_mul_sq_succ` | A,C,W,R | corrected and proved for width `k+1`: exactly `2^k` complementary patterns are scheduled, one literal mixed-polarity controlled block handles each pattern, and every diagonal phase is retained exactly. Each block costs between `(k+1)^2` and `56*(k+1)^2`, giving matching syntax-derived finite envelopes for the complete diagonal schedule; this is not an optimality claim. |
| U8-gray | pp. 27–28; lines 959–976 | Gray path and negative controls implement a two-level rotation | `basisPath` and endpoint/adjacency/nodup/length theorems; `patternControlledCircuit`; `endpointOrientedUnitary`; `eval_adjacentTwoLevelCircuit` | C,W,R | partially formalized: the shortest-path combinatorics, exact mixed-polarity restoration, and both adjacent endpoint orientations are proved. The paper's complete repeated move/apply/unmove circuit is not the main synthesis backend and is not represented by a named `2m-3`-macro circuit. |
| U8-pathcount | p. 28; lines 973–979 | each path uses `2m−3`, `m≤n+1` multi-control gates | `length_basisPath`; `length_basisPath_le`; `basisPath_edge_hammingDist`; `basisPath_isChain` | R | partially formalized: path length is exactly `hammingDist + 1` and at most `n+1`; no resource conclusion is inferred without the corresponding complete Gray-walk syntax. |
| U8-affine | pp. 27–28; lines 959–976 | realize a nonadjacent two-level block | `affinePairCircuit`; exact endpoint/basis-action theorems; exact X/CNOT/count/cost theorems; `unitary_conjugate_twoLevelUnitary`; `twoLevelCircuit`; `eval_twoLevelCircuit` | A,C,W,R | corrected and strengthened implementation: local X translation and common-pivot CNOT clearing normalize the ordered pair to zero/singleton, one adjacent block is applied, and the literal transport is reversed by its adjoint. This exact no-ancilla backend replaces the repeated Gray walk in the main library while preserving the source path layer for traceability. |
| U8-general | pp. 27–28; lines 939–983 | exact arbitrary n-qubit unitary, no work bits, claimed `Θ(n³4^n)` | `decomposeFinUnitary_factors_length`; `decomposeFiniteUnitary_factors_length`; `decomposeQubitUnitary_factors_length`; `twoLevelCircuitCost_bounds`; `FiniteTwoLevelFactor.circuitCost_bounds`; `exactSynthesisCircuit`; `eval_exactSynthesisCircuit`; `exactSynthesisCircuit_oneQubitCNOTCost`; `exactSynthesisCircuit_arbitraryTwoQubitCost` | A,C,W,R,O | corrected and proved for exact positive-width synthesis with no work bits: the fixed non-pruning eliminator emits exactly `Nat.choose (2^n) 2` factors, including scheduled identity factors, and each selected factor has a syntax-derived cost between `n^2` and `56*n^2`. The assembled literal one-qubit/CNOT circuit retains every phase and has the same exact cost under both named models. Width zero has a proved phase obstruction and width one a direct one-gate theorem. The source's cubic-per-factor `Theta` argument is replaced by the construction-specific quadratic result below, not by an optimal-synthesis claim. |

## Headline Result Assembly

| ID | Source | Paper content | Lean counterpart | Layers | Status / notes |
|---|---|---|---|---|---|
| T-universal | abstract, pp. 1/6/27–28 | all one-qubit U(2) gates + CNOT exactly generate all finite-qubit unitaries | `exact_oneQubitCNOT_universality`; `eval_exactSynthesisCircuit`; `eval_widthOneCircuit`; `no_basicCircuit_zero_realizes_neg` | A,C,W | corrected and proved: exact universality holds at every positive width in literal one-qubit/CNOT syntax; width one has a direct singleton circuit, while exact width-zero universality is false and has a certified counterexample. No fixed finite gate-set density or efficiency claim is bundled into this theorem. |
| T-universal-cost | abstract and §8 | resource upper bound for the explicit general construction | `exactSynthesisBenchmark`; `two_mul_exactSynthesisBenchmark_le_cost`; `exactSynthesisCost_bounds`; `exactSynthesisCircuit_gateCount`; `exactSynthesisCircuit_arbitraryTwoQubitCost`; `exact_arbitraryTwoQubit_universality`; `exactSynthesisCost_isBigOWith_fixedSchedule`; `exactSynthesisBenchmark_isBigOWith_cost`; `exactSynthesisCost_isTheta_fixedSchedule` | R,O | corrected and proved for the selected non-pruning syntax. With `k+1` wires and benchmark `B(k)=(k+1)^2*4^k`, every input satisfies the exact finite sandwich `2*B(k) ≤ cost ≤ 112*B(k)`; that cost is also the literal gate count and is preserved by the broader Section 8 model without merging gates. Every unitary family synthesized by this fixed implementation therefore has cost `Theta(B(k))`. This is neither a lower bound on arbitrary circuits nor an optimality theorem, and it does not establish the source's six-`U(4)` or dimension-counting claims. |
