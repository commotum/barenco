# Goal 2 Continuation Prompt

```text
Work autonomously through /Users/jake/Developer/barenco/goal-2/0-plan.md using
/Users/jake/Developer/barenco/goal-2/0-loop.md and the repository-wide
/Users/jake/Developer/barenco/BUILD-PLAN.md.

The objective is to add certified arbitrary two-wire unitary embeddings and
trusted circuit syntax, develop a payload-preserving exact fusion/normalization
layer with syntax-derived model-specific resource proofs, and use it on the
paper's disputed relative-phase Toffoli cost-three construction, Lemma 7.1 Gray
post-merger count, and Corollary 7.4 optimized count.

Inspect the actual repository and update current facts before each stage. Work on
the first incomplete stage only, create its stage file from the loop template,
keep modules narrow, run focused and adjacent verification, record exact results,
fold them into 0-plan.md, and continue.

Do not infer locality from Primitive metadata, forge an arbitrary-two-qubit tag,
use unclassified full-register gates as fused syntax, infer a gate count from a
matrix equality, discard scalar phase, conflate the two cost models, hard-code a
disputed formula, use an opaque choice circuit as optimizer-visible factors, or
call optimizer failure a disproof. Completed modules may contain no sorry, admit,
by?, native_decide, bv_decide, custom axiom, opaque, sorryAx, or implemented_by.

Completion means the original three-part objective is actually implemented and
audited: public two-wire semantics/syntax, an executable exact sound normalizer
with proved cost behavior, and named syntax/evaluator/count outcomes for every
test case. A paper claim may finish as verified, rigorously refuted, or explicitly
not recovered with the strongest checked result; open issues must remain visible
as concrete next work rather than being silently weakened or declared solved.
```
